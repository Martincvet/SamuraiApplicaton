@page
@model SamuraiApplicaton.Pages.Battle.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Battle</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Battle.Id" />
            <div class="form-group">
                <label asp-for="Battle.Name" class="control-label"></label>
                <input asp-for="Battle.Name" class="form-control" />
                <span asp-validation-for="Battle.Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Battle.StartDate" class="control-label"></label>
                <input asp-for="Battle.StartDate" class="form-control" />
                <span asp-validation-for="Battle.StartDate" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Battle.EndDate" class="control-label"></label>
                <input asp-for="Battle.EndDate" class="form-control" />
                <span asp-validation-for="Battle.EndDate" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Battle.Place" class="control-label"></label>
                <input asp-for="Battle.Place" class="form-control" />
                <span asp-validation-for="Battle.Place" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a class="btn btn-primary" asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
